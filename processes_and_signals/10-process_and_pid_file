#!/usr/bin/env bash
#Process and PID file
trap 'echo "I hate the kill command"' 15
trap 'echo "Y U no love me?"' 2
trap 'rmdir /var/run/myscript.pid' (( 3 || 15 ))
while true
  do
    echo "To infinity and beyond"
    sleep 2
  done

